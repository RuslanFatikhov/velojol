# Copy to .deploy.env and set the SSH destination. Do not commit real secrets.
DEPLOY_TARGET=root@example.com

# Optional overrides:
REMOTE_APP_ROOT=/opt/open-velojol
REMOTE_CURRENT=/opt/open-velojol/current
SERVICE_NAME=velojol-gunicorn.service
HEALTHCHECK_URL=http://127.0.0.1:8020
